using System;
using System.Threading.Tasks;
using TMPro;
using UnityEngine;
using UnityEngine.Events;

namespace Base.Screen
{
    public class TextPresenter : MonoBehaviour
    {
        /*[SerializeField] private Animator _animator;
        [SerializeField] private TextMeshProUGUI _text;
        [SerializeField] private float _delay = 0.5f;
        public UnityEvent OnTaskComplete;

        public async void SetTask(GeneratorTask task)
        {
            _animator.SetTrigger("Show");
            _text.text = $"Collect {task.CurrentCount} {task.CurrentType}";
            await Task.Delay(TimeSpan.FromSeconds(_delay));
            OnTaskComplete?.Invoke();
        } 
        public void SetText(string text)
        {
            _animator.SetTrigger("Show");
            _text.text = text;
        }*/
    }
}